@model IEnumerable<Acciones.Models.Municipios>

@{
    ViewData["Title"] = "Index";
}

<h1>Listado de Municipios</h1>

<p>
    <a asp-action="Create">Nuevo Registro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.FechaRegistro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaUltimoCambio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsuarioRegistro)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaUltimoCambio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsuarioRegistro)
            </td>*@
        <td>
            <!--<a asp-action="Edit" asp-route-id="@item.MunicipioId">Editar</a> |-->
            @*<a asp-action="Details" asp-route-id="@item.MunicipioId">Details</a> |*@
            <!--<a asp-action="Delete" asp-route-id="@item.MunicipioId">Eliminar</a>-->

            <a asp-action="Edit" asp-route-id="@item.MunicipioId"><span style='cursor: pointer' class='fas fa-edit' title='Editar'></span></a>
            <a asp-action="Delete" asp-route-id="@item.MunicipioId"><span style='cursor: pointer' class='fas fa-trash' title='Eliminar'></span></a>

        </td>
        </tr>
}
    </tbody>
</table>
